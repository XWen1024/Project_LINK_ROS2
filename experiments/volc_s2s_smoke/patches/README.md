# Vendor patches

No official SDK source patch is currently required. If an ARM64 portability
patch becomes necessary, add it here rather than editing the SDK submodule
silently, and document the reason, exact change, and upstream suitability.
